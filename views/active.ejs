<%- include('partials/header') %>

<h2 class="text-2xl font-semibold mb-4">📈 Active Trades</h2>

<ul class="space-y-4">
  <% Object.values(activeTrades).forEach(trade => { %>
    <li class="bg-white rounded-lg shadow p-4">
      <div class="flex justify-between items-center">
        <div>
          <p class="font-bold"><%= trade.name %></p>
          <p class="text-sm text-gray-600">Address: <%= trade.address %></p>
          <p class="text-sm text-gray-600">Entered: <%= new Date(trade.entryTime).toLocaleString() %></p>
        </div>
        <div class="text-right">
          <p class="text-sm">Entry Price: $<%= trade.entryPrice %></p>
          <p class="text-xs text-gray-500"><%= trade.simulation ? "🧪 Simulation" : "🟢 Live" %></p>
        </div>
      </div>
    </li>
  <% }) %>
</ul>

<%- include('partials/footer') %>