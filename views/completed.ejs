<%- include('partials/header') %>

<h2 class="text-2xl font-semibold mb-4">✅ Completed Trades</h2>

<ul class="space-y-4">
  <% completedTrades.forEach(trade => { %>
    <li class="bg-white rounded-lg shadow p-4">
      <div class="flex justify-between items-center">
        <div>
          <p class="font-bold"><%= trade.name %></p>
          <p class="text-sm text-gray-600">Address: <%= trade.address %></p>
          <p class="text-sm text-gray-600">
            Entry: $<%= trade.entryPrice %> → Exit: $<%= trade.exitPrice %>
          </p>
          <p class="text-sm text-gray-600">Exited: <%= new Date(trade.exitTime).toLocaleString() %></p>
        </div>
        <div class="text-right">
          <p class="<%= trade.loss ? 'text-red-600' : 'text-green-600' %> font-semibold">
            <%= trade.loss ? '💀 LOSS' : '💰 PROFIT' %> <%= trade.gain %>%
          </p>
          <p class="text-xs text-gray-500"><%= trade.simulation ? "🧪 Simulation" : "🟢 Live" %></p>
        </div>
      </div>
    </li>
  <% }) %>
</ul>

<%- include('partials/footer') %>